diff --git a/tests/test_pi_bridge/test_sidecar_manager_extra.py b/tests/test_pi_bridge/test_sidecar_manager_extra.py
index 0447eea..6a3b4f4 100644
--- a/tests/test_pi_bridge/test_sidecar_manager_extra.py
+++ b/tests/test_pi_bridge/test_sidecar_manager_extra.py
@@ -83,10 +83,10 @@ class TestResolveBunPath:
         monkeypatch.setattr("config.settings.get_settings", boom)
         assert _resolve_bun_path() == _DEFAULT_BUN_PATH
 
-    def test_default_bun_path_is_absolute(self):
-        # 生产环境契约：默认路径应指向一个绝对路径
+    def test_default_bun_path_is_absolute_or_path_resolvable(self):
+        # 默认路径可以是绝对路径，或 "bun"（由 PATH 解析）
         import os
-        assert os.path.isabs(_DEFAULT_BUN_PATH)
+        assert os.path.isabs(_DEFAULT_BUN_PATH) or _DEFAULT_BUN_PATH == "bun"
 
 
 # ---------------------------------------------------------------------------
