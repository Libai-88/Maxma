diff --git a/web/src/composables/useTheme.ts b/web/src/composables/useTheme.ts
index acc56e0..87ec2f4 100644
--- a/web/src/composables/useTheme.ts
+++ b/web/src/composables/useTheme.ts
@@ -133,15 +133,15 @@ function onSystemThemeChange(e: MediaQueryListEvent) {
   systemIsDark.value = e.matches
 }
 // 在 MediaQueryList 对象上挂载标记，此对象在 HMR 中存活，可防重复注册
-if (!(systemMql as Record<string, unknown>)._themeListenerAttached) {
-  systemMql.addEventListener('change', onSystemThemeChange)
-  ;(systemMql as Record<string, unknown>)._themeListenerAttached = true
-}
+	if (!(systemMql as unknown as Record<string, unknown>)._themeListenerAttached) {
+	  systemMql.addEventListener('change', onSystemThemeChange)
+	  ;(systemMql as unknown as Record<string, unknown>)._themeListenerAttached = true
+	}
 
 /** 移除系统主题变化监听器（用于清理 / HMR / 测试） */
 export function cleanupThemeListener() {
   systemMql.removeEventListener('change', onSystemThemeChange)
-  ;(systemMql as Record<string, unknown>)._themeListenerAttached = false
+	;(systemMql as unknown as Record<string, unknown>)._themeListenerAttached = false
 }
 
 /** 当前实际生效的主题（auto 解析后） */
