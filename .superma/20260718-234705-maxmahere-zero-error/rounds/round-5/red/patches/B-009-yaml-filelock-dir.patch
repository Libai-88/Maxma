diff --git a/api/yaml_store.py b/api/yaml_store.py
index bfab3ae..1014996 100644
--- a/api/yaml_store.py
+++ b/api/yaml_store.py
@@ -80,11 +80,11 @@ def yaml_file_lock(path: str | Path, timeout: int = 5) -> Iterator[None]:
     path_str = str(Path(path))
     inproc_lock = _get_inproc_lock(path_str)
     inproc_lock.acquire()
+    lock_path = _lock_path(path)
+    lock_path.parent.mkdir(parents=True, exist_ok=True)
     try:
         if _check_portalocker():
             import portalocker
-            lock_path = _lock_path(path)
-            lock_path.parent.mkdir(parents=True, exist_ok=True)
             with portalocker.Lock(str(lock_path), timeout=timeout):
                 yield
         else:
