diff --git a/web/src/api/index.ts b/web/src/api/index.ts
index 47476f1..20b7c08 100644
--- a/web/src/api/index.ts
+++ b/web/src/api/index.ts
@@ -82,7 +82,6 @@ export function getToken(): string {
  */
 export async function ensureTokenLoaded(): Promise<void> {
   if (tokenFetchedAtRuntime) return
-  const myVersion = tokenLoadVersion
   if (!tokenLoadPromise) {
     const capturedVersion = tokenLoadVersion
     tokenLoadPromise = (async () => {
@@ -123,11 +122,13 @@ export async function ensureTokenLoaded(): Promise<void> {
       )
     })()
   }
+  const capturedPromise = tokenLoadPromise
   try {
-    await tokenLoadPromise
+    await capturedPromise
   } finally {
-    // 仅在版本未变化时清除 promise，防止 resetToken() 后旧 finally 误清新创建的 promise
-    if (tokenLoadVersion === myVersion) {
+    // 仅当当前 promise 仍是本调用创建的 promise 时才清除，防止 resetToken()
+    // 或并发 ensureTokenLoaded() 误清洗新创建的 promise。
+    if (tokenLoadPromise === capturedPromise) {
       tokenLoadPromise = null
     }
   }
