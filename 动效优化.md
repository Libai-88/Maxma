# MaxmaHere 前端动效优化方案（GSAP 化）

> **目标读者**：下一个没有任何上下文的 agent / 工程师。读完本文应能立即动手实施每一个优化点。
> **范围**：仅限 Web 前端（`web/src/`），不涉及 Python 后端 / Tauri 壳 / Bun sidecar。
> **技术栈基线**：Vue 3.5 + Pinia + TypeScript + Vite + vue-router（Vue 3，不是 React）。
> **GSAP skills 来源**：`D:\Maxma\.agents\skills\`（gsap-core / gsap-timeline / gsap-scrolltrigger / gsap-plugins / gsap-utils / gsap-frameworks / gsap-performance）。**不要用 gsap-react**（那是 React 模式）。
> **当前版本基线**：`v2.6.6` / `c22feee3` / `feat/omp-alignment`。

---

## 一、为什么要用 GSAP

**现状一句话总结**：项目已有不错的 CSS 动画基础设施（`web/src/assets/styles/animations.css` 集中定义 30+ keyframes、`tokens.css` 提供 6 档缓动曲线、`@starting-style` 已经用上），但**所有时序都是手工编织**——缺 timeline、缺 FLIP、缺 stagger 工具、缺字符级揭示、缺滚动联动、缺跨组件协调。

**GSAP 带来的核心增量**（按本项目实际收益排序）：

| 增量 | 当前痛点 | 项目落地点 |
|---|---|---|
| `gsap.context()` 自动 cleanup | 路由切换常残留未清理的事件监听 / setTimeout | 所有视图动画统一收口 |
| `gsap.timeline()` 多步编排 | MessageBubble、ToolCallCard、WelcomeScreen 的入场都是离散动画 | 聊天流式编排 |
| 自动 FLIP | ToolCallCard / BubbleChrome 手动算 `scrollHeight` 设 `maxHeight` | 工具气泡展开/收起 |
| `gsap.utils.toArray` + `selector(scope)` | MessageBubble 用 `--stagger-delay` 手工注入延迟 | 消息入场 stagger |
| `gsap.matchMedia()` 响应 `prefers-reduced-motion` | 现在每个组件都写 `@media (prefers-reduced-motion: reduce)` | 统一收口 |
| `ScrollTrigger` 滚动驱动 | MemoryView 长列表 / CapabilitiesView 长配置源没有滚动揭示 | 滚动入场 |
| `SplitText` 字符级揭示 | AI 回复流式只是文本替换，无字符动画 | AI 回复打字效果 |
| `Observer` 滚轮监听 | 桌面应用没有惯性滚动 | 长内容浏览 |
| `quickTo` 高频更新 | 没用 | 鼠标跟随等 |
| `gsap.utils.distribute` | 没用 | MemoryView 置信度可视化 |

**不引入 GSAP 的反例**（不要过度工程化）：

- 简单 hover / focus 反馈 → 保留 CSS transition
- 主题切换（颜色、背景）→ 保留现有 CSS 过渡
- 单元素 icon spin / pulse → 保留 CSS @keyframes
- 路由级 keep-alive 的 router-view 切换 → 用 CSS `<Transition>` 包裹 component，不引入 GSAP

---

## 二、现有动画现状盘点

实施前必须先读懂现状。本节列出所有动画相关代码的入口，下一节开始逐项优化。

### 2.1 全局动画基础设施（保留）

| 路径 | 角色 | 是否改动 |
|---|---|---|
| `web/src/assets/styles/animations.css` | 30+ `@keyframes maxma-*` 集中定义 | **保留**，GSAP 不替代 keyframes，仅取代 JS 编排 |
| `web/src/assets/styles/tokens.css` | 6 档缓动曲线 + 3 档时长 token | **保留**，GSAP 的 `ease` 参数复用这里定义的曲线 |
| `web/src/assets/styles/design-system.css` | 设计系统样式 | 不动 |
| `web/src/assets/styles/paper-texture.css` | 纸质纹理 | 不动 |
| `web/src/themes/*.css` | 6 个主题（素影、超线、夜晚、金缮、草绿、子夜） | 不动 |

### 2.2 现有动画使用模式盘点（按文件）

| 文件 | 现有动画方式 | 痛点 / 可优化点 |
|---|---|---|
| `composables/useTypewriter.ts` | 手写 `setTimeout` 打字机 | 仅用于欢迎屏空态的"今天想聊点什么"，可改用 SplitText 或保留（仅一处） |
| `composables/useChatScroll.ts` | 用 JS 变量 + `--stagger-delay` 注入 CSS transition-delay | 600ms 窗口 + 40ms × index stagger，可改用 `gsap.to({ stagger })` |
| `components/MessageBubble.vue` | CSS `@starting-style` + transition + `--stagger-delay` 入场，hover transform | 缺乏方向感统一、缺乏字符级揭示 |
| `components/ThinkingBlock.vue` | CSS spinner + transition | 已 OK，spinner 不动 |
| `components/ChatInput.vue` | `<TransitionGroup ref-tag>` + Vue Transition `quote-pop` + 错误横幅 CSS keyframe | ref-tag 入场可改 stagger；引用浮动按钮可改 GSAP |
| `components/ChatInput.vue` 的 send 按钮 | `maxma-send-success` / `maxma-send-shake` CSS keyframes | 已 OK，但 scale 弹簧效果可改 GSAP spring |
| `components/ToolCallCard.vue` + `tools/_shared/BubbleChrome.vue` | **手算 `maxHeight = scrollHeight + 'px'`**，监听 `isOpen` watcher | **🔴 核心痛点**：触发多次 layout、scrollHeight 取值时机依赖、`none` 设回去后尺寸变化时再触发的潜在闪烁 |
| `components/WelcomeScreen.vue` | 一系列 CSS `@keyframes welcome-fade-in` + 延迟 | 顺序延迟硬编码（0 / 0.12 / 0.24 / ...），改 stagger 后可统一配置 |
| `components/FloatSidebar.vue` | CSS `fs-slide-in/out` | 已 OK，但 Vue 路由切换缺过渡 |
| `components/SessionDrawer.vue` | `<Transition name="session-drawer">` | drawer 滑入已 OK，但 backdrop 渐显可优化 |
| `components/SessionSidebar.vue` | `<Transition name="card">`、`constify-pop` | hover 卡片可优化 |
| `components/workbench/WorkbenchPanel.vue` | `<Transition name="workbench">` | 工作台展开已 OK，可加 stagger 子元素 |
| `views/CapabilitiesView.vue` | 无入场动画（直接渲染） | 统计数字 0→N 应有 reveal |
| `views/MemoryView.vue` | 无入场动画 | 记忆列表 fade-up + stagger 缺失 |
| `views/ChatView.vue` | 无聊天页面入场动画 | 首次进入聊天应有节奏感 |
| `router/index.ts` | `router.afterEach` 仅 `window.scrollTo(0, 0)` | 路由级 transition 缺失 |
| `App.vue` | `<router-view>` + `<keep-alive include="ChatView">` | `<Transition>` 缺失 |

### 2.3 关键设计 token（必须复用）

```css
/* tokens.css 中的动效相关 token — GSAP 也使用这些 */
--duration-instant: 0.1s;
--duration-fast:    0.15s;
--duration-slow:    0.25s;
--ease-out:      cubic-bezier(0.23, 1, 0.32, 1);
--ease-in:       cubic-bezier(0.7, 0, 0.84, 0);
--ease-standard: cubic-bezier(0.77, 0, 0.175, 1);
--ease-smooth:   cubic-bezier(0.22, 0.68, 0, 1);
--ease-drawer:   cubic-bezier(0.32, 0.72, 0, 1);
--ease-spring:   cubic-bezier(0.34, 1.56, 0.64, 1);
```

GSAP 用这些 token 的方式见 §3.1 的 `useGsap.ts` 设计。

---

## 三、详细优化方案

每个优化点独立成节，包含：**现状 → 问题 → 方案（含代码）→ 新旧对比 → 验收标准**。

---

### 🎯 优化点 1：建立 GSAP 基础设施（核心）

**优先级**：P0 — 一切优化的前置依赖。必须先做完才能做后续。

#### 现状

项目目前**没有任何 GSAP 依赖**。8 个 GSAP skills 都未引用。组件里只有 CSS transition / Vue `<Transition>` / 手写 setTimeout。

#### 问题

- GSAP 没有 Vue 3 适配层 → 直接 `import { gsap } from 'gsap'` 在 setup() 里写会内存泄漏（路由切换时 tween 不清理）
- 没有统一 `prefers-reduced-motion` 收口 → 每个组件都写重复的 `@media` 块
- 没有统一的 ease / duration 复用 → 不一致

#### 方案

**1) 安装依赖**

```bash
cd web
npm install gsap @gsap/business           # 包含全部免费插件（含 SplitText）
# 注：Webflow 收购后所有插件免费，包括原 ClubGreenSock 的 SplitText / MorphSVG / DrawSVG 等
```

**2) 创建 composable 基础设施**

文件：`web/src/composables/useGsap.ts`（新建）

```typescript
import { gsap } from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'
import { SplitText } from 'gsap/SplitText'
import { Observer } from 'gsap/Observer'
import { onScopeDispose, getCurrentScope } from 'vue'

/* eslint-disable @typescript-eslint/no-explicit-any */

// 单例注册（项目只需注册一次）
let registered = false
function ensureRegistered() {
  if (registered) return
  gsap.registerPlugin(ScrollTrigger, SplitText, Observer)
  // matchMedia 全局默认尊重 prefers-reduced-motion
  gsap.matchMedia().add('(prefers-reduced-motion: reduce)', () => {
    // 所有 timeline / tween 在 reduce 模式下 0.01s 完成
    gsap.globalTimeline.timeScale(1000)
    return () => { gsap.globalTimeline.timeScale(1) }
  })
  registered = true
}

export interface UseGsapOptions {
  scope?: () => HTMLElement | undefined | null
}

/**
 * Vue 3 GSAP composable — 模仿 @gsap/react 的 useGSAP 行为：
 *   1) 自动 ctx.revert() 在组件卸载时（onScopeDispose）
 *   2) scope 选择器限定到组件根元素
 *   3) contextSafe 包装事件回调
 */
export function useGsap(
  setup: (ctx: gsap.Context, contextSafe: <T extends (...args: any[]) => any>(fn: T) => T) => void | (() => void),
  options: UseGsapOptions = {},
) {
  ensureRegistered()

  const ctx = gsap.context(setup, options.scope?.() ?? undefined)

  // 将 setup 返回的 cleanup 也注册进去
  let cleanup: (() => void) | void
  const wrapped = (c: gsap.Context, safe: any) => {
    cleanup = setup(c, safe)
  }
  // 重新执行一次拿到 cleanup（因为 ctx 会包装）
  // 实际更简洁的做法：把 setup 返回值存到 ctx 上
  // 为简洁起见，下面用 onScopeDispose 处理
  void wrapped

  if (getCurrentScope()) {
    onScopeDispose(() => {
      if (typeof cleanup === 'function') cleanup()
      ctx.revert()
    })
  }

  return ctx
}

/**
 * 全局 token → GSAP 参数映射（与 tokens.css 保持一致）
 */
export const easeMap = {
  out:      'power3.out',       // 对应 --ease-out
  in:       'power3.in',        // 对应 --ease-in
  standard: 'power2.inOut',     // 对应 --ease-standard
  smooth:   'power2.out',       // 对应 --ease-smooth
  drawer:   'power3.out',       // 对应 --ease-drawer（无完全对应 GSAP 内置）
  spring:   'back.out(1.7)',    // 对应 --ease-spring 的弹性
} as const

export const durationMap = {
  instant: 0.1,
  fast:    0.15,
  slow:    0.25,
} as const

export { gsap, ScrollTrigger, SplitText, Observer }
```

**3) 类型增强**

文件：`web/src/types/gsap.d.ts`（新建，可选）

```typescript
import 'gsap'
declare module 'gsap' {
  interface Context {
    /** 标记 setup 返回的清理函数，由 useGsap 在 dispose 时调用 */
    __cleanup?: () => void
  }
}
```

#### 新旧对比

| 维度 | 旧 | 新 |
|---|---|---|
| 内存清理 | 手写 `onUnmounted(() => clearTimeout(...))` | `useGsap` 自动 `ctx.revert()` 撤销所有 tween + ScrollTrigger |
| 作用域 | selector 字符串全局匹配（极易越界） | `scope: () => rootRef` 限定到组件根 |
| reduce-motion | 每个组件写 `@media` 块 | 全局 `matchMedia` 一行收口 |
| ease/duration | 各处写 `cubic-bezier(...)` 字面量 | 通过 `easeMap` / `durationMap` 引用 token |

#### 验收标准

- ✅ `web/src/composables/useGsap.ts` 文件存在，TypeScript 编译通过
- ✅ `package.json` 增加 `gsap` 和 `@gsap/business` 依赖
- ✅ 写一个最小示例组件验证 `useGsap(() => gsap.to('.box', { x: 100 }))` 在组件卸载后停止动画（DevTools Performance 录制可证）
- ✅ 设置系统级 reduce-motion 后，调用 `gsap.to(...)` 立即完成（duration ≈ 0.01s）

---

### 🎯 优化点 2：工具气泡展开/收起 — 用 GSAP FLIP 替换手算 maxHeight

**优先级**：P0 — 影响每个工具调用的展开体验，覆盖率最高。

#### 现状

`web/src/components/tools/_shared/BubbleChrome.vue` 第 41-62 行：

```vue
<script setup lang="ts">
function openBody() {
  if (!bodyWrapper.value) return
  const h = bodyWrapper.value.scrollHeight
  bodyWrapper.value.style.maxHeight = h + 'px'
  setTimeout(() => {
    if (bodyWrapper.value && isOpen.value) {
      bodyWrapper.value.style.maxHeight = 'none'
    }
  }, 350)  // ← 硬编码 350ms，与 CSS transition 的 0.3s 强绑定
}

watch(isOpen, (open) => {
  if (!bodyWrapper.value) return
  if (open) {
    openBody()
  } else {
    bodyWrapper.value.style.maxHeight = bodyWrapper.value.scrollHeight + 'px'
    void bodyWrapper.value.offsetHeight      // ← 强制 reflow hack
    bodyWrapper.value.style.maxHeight = '0px'
  }
})
```

`web/src/components/ToolCallCard.vue` 第 350-378 行有**几乎相同**的实现。

#### 问题

- 🔴 **强制 reflow**（`void bodyWrapper.value.offsetHeight`）在每次 toggle 时触发，破坏浏览器渲染管线
- 🔴 **硬编码 350ms** 与 CSS transition 不同步，时长漂移风险
- 🔴 **内容动态增长时**（如 streaming output 追加）`scrollHeight` 重新计算，已设 `none` 的 maxHeight 会让内容溢出而无动画
- 🔴 **`ToolCallCard.vue` 重复实现**同一套逻辑，是维护负担

#### 方案

**1) `BubbleChrome.vue` 改造（核心共享组件，影响 10+ 工具气泡）**

```vue
<script setup lang="ts">
import { ref, watch, onMounted } from 'vue'
import type { ToolCall } from '@/types'
import { toolDisplayName } from './displayNames'
import Icon from '@/components/Icon.vue'
import { gsap, useGsap, easeMap, durationMap } from '@/composables/useGsap'

const props = defineProps<{ toolCall: ToolCall }>()

const bodyWrapper = ref<HTMLElement | null>(null)
const isOpen = ref(false)

// 把展开/收起包成可重入函数
function animateOpen() {
  if (!bodyWrapper.value) return
  const el = bodyWrapper.value
  gsap.set(el, { height: 'auto' })
  const target = el.offsetHeight
  gsap.fromTo(el,
    { height: 0, opacity: 0 },
    { height: target, opacity: 1, duration: durationMap.slow,
      ease: easeMap.out, overwrite: 'auto' }
  )
}

function animateClose() {
  if (!bodyWrapper.value) return
  const el = bodyWrapper.value
  gsap.to(el, {
    height: 0, opacity: 0, duration: durationMap.fast,
    ease: easeMap.out, overwrite: 'auto',
  })
}

useGsap(({ contextSafe }) => {
  // contextSafe 包装回调，确保组件卸载后回调不执行
  const onOpenChange = contextSafe((open: boolean) => {
    if (open) animateOpen()
    else animateClose()
  })

  watch(isOpen, onOpenChange)
}, { scope: () => bodyWrapper.value?.parentElement })

function toggle() {
  if (props.toolCall.status === 'running') return
  isOpen.value = !isOpen.value
}

onMounted(() => {
  if (props.toolCall.status === 'running') {
    isOpen.value = true
  }
})

watch(() => props.toolCall.status, (s) => {
  if (s === 'running') isOpen.value = true
})
</script>

<style scoped>
/* 删掉所有 .bubble-body-wrapper 的 max-height transition */
.bubble-body-wrapper {
  overflow: hidden;
  /* height 由 GSAP 控制；初始高度为 0 */
  height: 0;
  opacity: 0;
}
</style>
```

**2) `ToolCallCard.vue` 改造**

第 350-378 行删除 `watch(isOpen, ...)` 块，改为同样的 `gsap.fromTo`。这样两个组件共享同一套 `useGsap` 调用模式。

#### 新旧对比

| 场景 | 旧行为 | 新行为 |
|---|---|---|
| 展开一个 800px 内容的气泡 | 设置 maxHeight=800px，350ms 后设 none | GSAP 测量 offsetHeight 后 fromTo 动画 0→800 |
| 收起气泡 | 强制 reflow + 设 0 | gsap.to height→0，无 reflow |
| 流式输出追加新内容（高度变化） | 已设 none 的 maxHeight 让内容溢出，无动画 | 不依赖 maxHeight，height: auto 后 GSAP 重新测量 |
| 路由切换回退 | ScrollTrigger / timer 残留 | `ctx.revert()` 自动撤销 |
| 维护成本 | BubbleChrome + ToolCallCard 重复实现 | 两处都走 `useGsap` 同一套 |

#### 验收标准

- ✅ 工具气泡展开/收起动画与现有 CSS transition 等时长（0.25s）、同缓动（cubic-bezier(0.23, 1, 0.32, 1)）
- ✅ 在 Chrome DevTools Performance 录制里**看不到** "Forced reflow" 警告
- ✅ 流式工具输出（tool_execution_update）追加时，容器高度平滑跟随（不抖动）
- ✅ `prefers-reduced-motion: reduce` 下展开/收起为瞬时（GSAP 时间被 1000× 缩放）

---

### 🎯 优化点 3：消息入场 stagger — 用 GSAP 替换 --stagger-delay 注入

**优先级**：P0 — 聊天主流程的视觉节奏感。

#### 现状

`web/src/composables/useChatScroll.ts` 第 29-45 行：

```typescript
const staggerWindowOpen = ref(true)
let staggerTimer: ReturnType<typeof setTimeout> | null = null

function openStaggerWindow() {
  staggerWindowOpen.value = true
  if (staggerTimer) clearTimeout(staggerTimer)
  staggerTimer = setTimeout(() => {
    staggerWindowOpen.value = false
    staggerTimer = null
  }, 600)
}

function turnStaggerStyle(idx: number): { '--stagger-delay': string } | undefined {
  if (!staggerWindowOpen.value || idx === 0) return undefined
  return { '--stagger-delay': `${Math.min(idx, 10) * 40}ms` }
}
```

`web/src/components/MessageBubble.vue` 第 137-148 行消费 `--stagger-delay`：

```css
.message-row {
  opacity: 1;
  transform: translateX(0);
  transition: opacity 0.15s var(--ease-out),
              transform 0.15s var(--ease-out);
  transition-delay: var(--stagger-delay, 0s);
  @starting-style {
    opacity: 0;
    transform: translateX(var(--row-slide-x, 12px));
  }
}
```

#### 问题

- 🟡 `transition-delay` 不能中断：如果用户中途切走再切回，动画状态错乱
- 🟡 600ms 窗口 + 40ms × index 是硬编码；想换节奏要改两处（composable + CSS）
- 🟡 `@starting-style` 现代浏览器（Chrome 117+）才支持，WebView2 版本可能落后
- 🟡 缺失字符/单词级揭示

#### 方案

**1) `useChatScroll.ts` 改造**

```typescript
import { gsap, useGsap, easeMap } from '@/composables/useGsap'

// 保留 staggerWindowOpen 概念，但用 GSAP 而非 setTimeout
function playStaggerForNewTurns(rows: HTMLElement[]) {
  if (rows.length === 0) return
  // 限制最多前 10 条，之后连续无延迟
  const targets = rows.slice(0, 10)
  gsap.from(targets, {
    opacity: 0,
    x: (i, el) => el.classList.contains('user') ? 12 : -12,
    duration: 0.25,
    ease: easeMap.out,
    stagger: 0.04,  // 40ms / 项
  })
}
```

**2) `MessageBubble.vue` 改造**

```vue
<script setup lang="ts">
import { onMounted, ref } from 'vue'
import { gsap, useGsap, easeMap } from '@/composables/useGsap'

const rowEl = ref<HTMLElement | null>(null)

useGsap(() => {
  // 入场动画：方向由 role 决定
  if (rowEl.value) {
    gsap.from(rowEl.value, {
      opacity: 0,
      x: props.role === 'user' ? 12 : -12,
      duration: 0.25,
      ease: easeMap.out,
    })
  }
}, { scope: () => rowEl.value })
</script>

<template>
  <article ref="rowEl" class="message-row" :class="role">
    <!-- 内容不变 -->
  </article>
</template>

<style scoped>
/* 删掉 .message-row 的 @starting-style 和 transition-delay 相关 CSS */
/* 保留 hover transform 和 bubble 的 transition（它们是状态反馈，不是入场） */
.message-row {
  /* 入场动画由 GSAP 控制 */
}
.bubble:hover {
  /* hover 反馈保留 */
}
</style>
```

#### 新旧对比

| 维度 | 旧 | 新 |
|---|---|---|
| 时序控制 | CSS transition-delay + setTimeout 计时 | GSAP timeline（可中途 kill、restart） |
| 中断处理 | 切走再切回时动画状态错乱 | GSAP 自动覆盖（overwrite: 'auto'） |
| 缓动统一 | 各组件写 `var(--ease-out)` 字面量 | `easeMap.out` 与项目 token 对齐 |
| 浏览器兼容 | 依赖 `@starting-style`（Chrome 117+） | GSAP 不依赖，WebView2 任何版本都支持 |
| 字符级揭示 | 无 | 配合优化点 6 可加 SplitText |

#### 验收标准

- ✅ 连续 10 条消息依次入场，间隔 40ms
- ✅ 第 11 条起无延迟
- ✅ 切换会话时旧消息立即显示（不退场再入场）
- ✅ `prefers-reduced-motion` 下无入场动画

---

### 🎯 优化点 4：ChatInput 引用标签 TransitionGroup → GSAP

**优先级**：P1 — 用户高频操作（添加/移除文件引用）。

#### 现状

`web/src/components/ChatInput.vue` 第 52 行 + 第 803-821 行 CSS：

```vue
<TransitionGroup name="ref-tag" tag="div" class="file-refs-bar" @before-leave="freezeLeavePos">
```

```css
.ref-tag-enter-active {
  transition: opacity 0.25s ease-out, transform 0.25s ease-out;
}
.ref-tag-enter-from {
  opacity: 0;
  transform: translateY(12px);
}
.ref-tag-leave-active {
  transition: opacity 0.2s ease-out, transform 0.2s ease-out;
  position: absolute !important;
}
.ref-tag-leave-to {
  opacity: 0;
  transform: translateY(12px);
}
.ref-tag-move {
  transition: transform 0.25s ease-out;
}
```

#### 问题

- 🟡 TransitionGroup 的 `position: absolute !important` 是 hack：让退场元素脱离文档流避免占位塌陷，但 FLIP 计算依赖这个 hack
- 🟡 无法在多元素同时移除时做交叉动画
- 🟡 与优化点 3 风格不统一

#### 方案

替换为 GSAP：

```vue
<script setup lang="ts">
import { gsap, useGsap, easeMap } from '@/composables/useGsap'

const refsBarEl = ref<HTMLElement | null>(null)

function animateRefChange() {
  if (!refsBarEl.value) return
  const current = Array.from(refsBarEl.value.children) as HTMLElement[]
  gsap.from(current, {
    opacity: 0,
    y: 12,
    scale: 0.92,
    duration: 0.25,
    ease: easeMap.out,
    stagger: 0.04,
  })
}

// 监听 refs 变化触发入场
watch([stickerSegments, imageRefs, nonImageRefs], () => {
  nextTick(animateRefChange)
}, { deep: true })
</script>
```

#### 新旧对比

| 维度 | 旧 | 新 |
|---|---|---|
| `position: absolute !important` hack | 必须 | 不需要 |
| 多元素同时入场 | CSS 顺序 transition | `stagger: 0.04` 显式控制 |
| 错误时清理 | 无 | `ctx.revert()` 自动 |

#### 验收标准

- ✅ 添加/移除引用 tag 时入场动画平滑
- ✅ 多个 tag 同时添加时有 stagger 节奏感
- ✅ 退场时无位置塌陷

---

### 🎯 优化点 5：WelcomeScreen 入场编排

**优先级**：P1 — 用户首次进入产品的第一印象。

#### 现状

`web/src/components/WelcomeScreen.vue` 第 301-324 行：

```css
@media (prefers-reduced-motion: no-preference) {
  .welcome-avatar {
    animation: welcome-fade-in 0.6s var(--ease-out) both;
  }
  .welcome-name {
    animation: welcome-fade-in 0.6s var(--ease-out) 0.12s both;
  }
  .welcome-scene {
    animation: welcome-fade-in 0.6s var(--ease-out) 0.24s both;
  }
  .welcome-greeting {
    animation: welcome-fade-in 0.6s var(--ease-out) 0.36s both;
  }
  .welcome-rule {
    animation: welcome-rule-in 0.5s var(--ease-out) 0.48s both;
  }
  .welcome-actions {
    animation: welcome-fade-in 0.6s var(--ease-out) 0.56s both;
  }
  .example-prompts {
    animation: welcome-fade-in 0.6s var(--ease-out) 0.7s both;
  }
}
```

#### 问题

- 🟡 7 个独立 keyframe + 硬编码 delay 列表，新增/删除元素要手工维护列表
- 🟡 头像、姓名、问候语、装饰线、操作按钮、示例提示是依次浮现 — 但当前实现没法中途停掉或反向（用户点"随便聊聊"按钮时其他元素还在动画）

#### 方案

```vue
<script setup lang="ts">
import { gsap, useGsap, easeMap } from '@/composables/useGsap'
import { onMounted, ref } from 'vue'

const contentEl = ref<HTMLElement | null>(null)

useGsap(({ contextSafe }) => {
  onMounted(() => {
    if (!contentEl.value) return
    const tl = gsap.timeline({ defaults: { ease: easeMap.out, duration: 0.5 } })
    tl.from('.welcome-aura',     { opacity: 0, scale: 0.96, duration: 0.8 })
      .from('.welcome-avatar',   { opacity: 0, y: 16, scale: 0.9 }, '<0.1')
      .from('.welcome-name',     { opacity: 0, y: 12 }, '<0.1')
      .from('.welcome-scene',    { opacity: 0, y: 12 }, '<0.08')
      .from('.welcome-greeting', { opacity: 0, y: 12 }, '<0.08')
      .from('.welcome-rule',     { opacity: 0, scaleX: 0 }, '<0.05')
      .from('.welcome-actions',  { opacity: 0, y: 12 }, '<0.1')
      .from('.example-prompts',  { opacity: 0, y: 16 }, '<0.1')
  })
}, { scope: () => contentEl.value })

// 用户点击"随便聊聊"按钮时让其他元素优雅退场（可选）
const emit = defineEmits<{ start: [message: string] }>()
function handleStart(msg: string) {
  if (contentEl.value) {
    gsap.to(contentEl.value.querySelectorAll(':scope > *:not(.welcome-aura)'), {
      opacity: 0, y: -8, duration: 0.2, stagger: 0.02,
      onComplete: () => emit('start', msg),
    })
  } else {
    emit('start', msg)
  }
}
</script>

<template>
  <div ref="contentEl" class="welcome-content">
    <!-- 模板结构不变，仅删除所有 welcome-* 的 animation CSS -->
  </div>
</template>

<style scoped>
/* 删除 .welcome-avatar / .welcome-name / .welcome-scene / .welcome-greeting /
   .welcome-rule / .welcome-actions / .example-prompts 的 animation 属性。
   仅保留 hover / active 的 :not(:scope > *:not(.welcome-aura))-type 反馈。 */
</style>
```

#### 新旧对比

| 维度 | 旧 | 新 |
|---|---|---|
| 时序协调 | 7 个独立 keyframe 各自 delay | 1 个 timeline，`<` `+=` 算子对齐 |
| 点击退场 | 无 | 其他元素优雅退场后再 emit |
| 新增元素 | 加一行 CSS + delay | 在 timeline 上 `.from('.new-elem', {}, '<0.1')` |
| 调试 | 改一个 delay 全文件搜 | timeline 一处改 |

#### 验收标准

- ✅ 欢迎屏 7 个区块依次入场，节奏与现状一致（avatar→name→scene→greeting→rule→actions→examples）
- ✅ 点击按钮时其他元素优雅退场（200ms 渐出 + 微微上移）
- ✅ `prefers-reduced-motion: reduce` 下所有元素立即显示

---

### 🎯 优化点 6：AI 回复字符级揭示（SplitText）

**优先级**：P2 — 体验亮点，但工作量大。

#### 现状

`web/src/components/RenderMarkdown.vue` + 流式 token 累积机制（详见 `composables/streamTextSnapshots.ts`）：每次 token 到达直接替换文本，没有逐字符/逐词动画。

#### 方案

**前提**：仅对**纯文本段落**启用，对代码块、表格、列表不启用（避免破坏语法结构）。

```vue
<script setup lang="ts">
import { SplitText, gsap, useGsap, easeMap } from '@/composables/useGsap'
import { ref, watch, nextTick } from 'vue'

const props = defineProps<{ content: string; streaming?: boolean }>()
const textContainer = ref<HTMLElement | null>(null)

let splitInstance: SplitText | null = null
let prevWordCount = 0

useGsap(({ contextSafe }) => {
  watch(() => props.content, async () => {
    if (!props.streaming || !textContainer.value) return
    await nextTick()

    // 仅首次创建 SplitText，后续只对新字符做 from
    if (!splitInstance) {
      splitInstance = new SplitText(textContainer.value, {
        type: 'words,chars',
        wordsClass: 'word++',
        charsClass: 'char++',
      })
    }
    const currentChars = splitInstance.chars
    const newChars = currentChars.slice(prevWordCount)
    if (newChars.length > 0) {
      gsap.from(newChars, {
        opacity: 0,
        y: 8,
        duration: 0.3,
        ease: easeMap.out,
        stagger: 0.015,
      })
    }
    prevWordCount = currentChars.length
  }, { flush: 'post' })

  // 流式结束：撤销 SplitText（不破坏语义）
  watch(() => props.streaming, (streaming) => {
    if (!streaming && splitInstance) {
      splitInstance.revert()
      splitInstance = null
      prevWordCount = 0
    }
  })
}, { scope: () => textContainer.value })
</script>
```

#### 新旧对比

| 维度 | 旧 | 新 |
|---|---|---|
| 视觉感受 | 文本一次性替换（阅读"跳"感） | 字符渐显（阅读"流"感） |
| 性能 | DOM 文本节点替换 | SplitText 拆词 + GSAP 渐显 |
| 内存 | 无额外开销 | SplitText 实例 + 字符级 span（流式结束后 revert 释放） |

#### 验收标准

- ✅ 流式 AI 回复时新字符逐字浮现（stagger 15ms / 字符）
- ✅ 流式结束后字符级标记被 revert（DevTools Elements 面板确认无额外 span）
- ✅ 代码块 / 列表 / 表格内不触发字符动画
- ✅ Markdown 重新渲染（粗体、链接、标题）后字符动画不破坏 DOM 结构

---

### 🎯 优化点 7：CapabilitiesView 统计数字 reveal

**优先级**：P1 — 体现品牌质感。

#### 现状

`web/src/views/CapabilitiesView.vue` 第 19-44 行，6 个 `.stat-card` 直接渲染最终数字，无 reveal：

```vue
<div class="stat-card">
  <div class="stat-value">{{ system.session_count ?? 0 }}</div>
  <div class="stat-label">活跃会话</div>
</div>
```

#### 方案

用 `gsap.utils.toArray` 配合 `from` 入场，并让 stat-value 数字从 0 滚动到目标值：

```vue
<script setup lang="ts">
import { gsap, useGsap, easeMap } from '@/composables/useGsap'
import { onMounted, ref, watch } from 'vue'

const statsEl = ref<HTMLElement | null>(null)

useGsap(() => {
  // 等数据加载完才触发
  watch([system, tools, providers], () => {
    if (!statsEl.value) return
    const cards = gsap.utils.toArray<HTMLElement>('.stat-card', statsEl.value)
    gsap.from(cards, {
      opacity: 0,
      y: 16,
      duration: 0.4,
      ease: easeMap.out,
      stagger: 0.06,
    })

    // 数字 0 → target 滚动
    cards.forEach((card) => {
      const valEl = card.querySelector('.stat-value')
      if (!valEl) return
      const target = Number(valEl.textContent) || 0
      if (target === 0) return
      const proxy = { v: 0 }
      gsap.to(proxy, {
        v: target,
        duration: 0.8,
        ease: easeMap.out,
        onUpdate: () => { valEl.textContent = Math.round(proxy.v).toString() },
      })
    })
  }, { deep: true, flush: 'post' })
}, { scope: () => statsEl.value })
</script>
```

#### 新旧对比

| 维度 | 旧 | 新 |
|---|---|---|
| 首次出现 | 6 个数字瞬时显示 | 卡片错落浮入 + 数字从 0 滚动到目标 |
| 重新加载 | 无动画 | 同样有动画 |

#### 验收标准

- ✅ 统计卡片入场 60ms 错落
- ✅ 数字从 0 滚动到目标，800ms 完成
- ✅ 长配置源列表（13 项）的 source-row 也用同样模式

---

### 🎯 优化点 8：MemoryView 记忆卡 fade-up + stagger

**优先级**：P1 — 列表视图通用模式。

#### 现状

`web/src/views/MemoryView.vue` 第 114-143 行，`.fact-card` 列表直接渲染，无入场动画：

```vue
<div v-for="fact in facts" :key="fact.id" class="fact-card">
  <!-- 卡片内容 -->
</div>
```

#### 方案

```vue
<script setup lang="ts">
import { gsap, useGsap, easeMap } from '@/composables/useGsap'

const factListEl = ref<HTMLElement | null>(null)

useGsap(() => {
  watch(() => store.facts.length, () => {
    if (!factListEl.value) return
    const cards = gsap.utils.toArray<HTMLElement>('.fact-card', factListEl.value)
    gsap.from(cards, {
      opacity: 0,
      y: 12,
      duration: 0.3,
      ease: easeMap.out,
      stagger: 0.05,
    })
  }, { flush: 'post' })
}, { scope: () => factListEl.value })
</script>
```

#### 验收标准

- ✅ 首次进入 MemoryView 时记忆卡片错落入场（50ms 间隔）
- ✅ 搜索筛选后重新渲染同样有动画
- ✅ 行内编辑进入/退出不影响列表动画

---

### 🎯 优化点 9：路由级过渡（router-view transition）

**优先级**：P1 — 涉及所有视图切换的连贯感。

#### 现状

`web/src/App.vue` 第 23-27 行：

```vue
<router-view v-slot="{ Component }">
  <keep-alive include="ChatView" :max="5">
    <component :is="Component" />
  </keep-alive>
</router-view>
```

无 `<Transition>` 包裹。`router/index.ts` 第 193-195 行：

```typescript
router.afterEach((_to) => {
  window.scrollTo(0, 0)
})
```

`window.scrollTo(0, 0)` 是硬跳。

#### 方案

**1) `App.vue` 加路由级 transition**

```vue
<router-view v-slot="{ Component, route }">
  <Transition :name="`page-${route.meta.transition || 'fade'}`" mode="out-in">
    <keep-alive include="ChatView" :max="5">
      <component :is="Component" :key="route.fullPath" />
    </keep-alive>
  </Transition>
</router-view>
```

**2) `router/index.ts` 改造**

```typescript
router.afterEach((to, from) => {
  // 仅当新路由不在 keep-alive 缓存里时才平滑滚动
  if (to.name !== from.name) {
    // 平滑滚动：1) 对普通页面用 CSS scroll-behavior: smooth 已生效
    // 2) 对 ChatView（已 keep-alive）不做滚动
    window.scrollTo({ top: 0, behavior: 'smooth' })
  }
})
```

**3) 样式**

```css
/* App.vue 或 animations.css */
.page-fade-enter-active,
.page-fade-leave-active {
  transition: opacity 0.2s var(--ease-out, cubic-bezier(0.23, 1, 0.32, 1)),
              transform 0.2s var(--ease-out, cubic-bezier(0.23, 1, 0.32, 1));
}
.page-fade-enter-from {
  opacity: 0;
  transform: translateY(8px);
}
.page-fade-leave-to {
  opacity: 0;
  transform: translateY(-4px);
}
```

#### 验收标准

- ✅ MemoryView → ChatView 切换有 fade 过渡
- ✅ ChatView 因 keep-alive 不会重新挂载（保留输入状态）
- ✅ `prefers-reduced-motion: reduce` 下 transition: none

---

### 🎯 优化点 10：SessionDrawer / WorkbenchPanel 抽屉滑入（增强）

**优先级**：P2 — 已有的 Vue Transition 可用，但子元素可加 stagger。

#### 现状

`web/src/components/SessionDrawer.vue` 用 `<Transition name="session-drawer">`，CSS 在 `assets/styles/animations.css` 中定义。抽屉本身滑入已 OK，但内部会话列表的项没有 stagger。

#### 方案

```vue
<!-- SessionSidebar.vue -->
<script setup lang="ts">
import { gsap, useGsap, easeMap } from '@/composables/useGsap'

const sessionListEl = ref<HTMLElement | null>(null)

useGsap(() => {
  watch(() => [props.sessions.length, props.constSessions.length], () => {
    if (!sessionListEl.value) return
    const items = gsap.utils.toArray<HTMLElement>('.session-item', sessionListEl.value)
    gsap.from(items, {
      opacity: 0,
      x: -16,
      duration: 0.3,
      ease: easeMap.out,
      stagger: 0.025,
    })
  }, { flush: 'post' })
}, { scope: () => sessionListEl.value })
</script>
```

#### 验收标准

- ✅ 抽屉打开时会话列表项依次滑入
- ✅ 仅在 sessions 数量变化时触发（避免重复动画）

---

### 🎯 优化点 11：ChatWindow 长列表滚动揭示（ScrollTrigger）

**优先级**：P2 — 桌面应用长对话滚动场景。

#### 现状

`web/src/components/ChatWindow.vue` 渲染消息列表（VirtualScroller / DynamicScroller），用户滚到上方查看历史时没有 reveal 动画。

#### 方案

```vue
<script setup lang="ts">
import { gsap, ScrollTrigger, useGsap, easeMap } from '@/composables/useGsap'
import { nextTick, watch } from 'vue'

const messagesEl = ref<HTMLElement | null>(null)

useGsap(() => {
  watch(() => props.turns.length, async () => {
    await nextTick()
    if (!messagesEl.value) return
    const rows = gsap.utils.toArray<HTMLElement>('.message-row', messagesEl.value)
    rows.forEach((row) => {
      ScrollTrigger.create({
        trigger: row,
        scroller: messagesEl.value,
        start: 'top 95%',
        once: true,
        animation: gsap.from(row, {
          opacity: 0,
          y: 12,
          duration: 0.4,
          ease: easeMap.out,
        }),
      })
    })
    ScrollTrigger.refresh()  // 重新计算位置
  }, { flush: 'post' })
}, { scope: () => messagesEl.value })
```

#### 注意事项

- WebView2 的 scroll 事件行为与 Chrome 一致，ScrollTrigger 可用
- 但 VirtualScroller 的虚拟列表会**销毁 / 复用** DOM 节点 → ScrollTrigger 实例必须配套销毁。在 `useGsap` 的 `ctx.revert()` 里会自动撤销。
- 若发现性能问题可降级为只在视口前 5 条做 reveal。

#### 验收标准

- ✅ 向上滚动查看历史时，滚入视口的消息有 fade-up 揭示
- ✅ 虚拟列表动态加载新消息时 ScrollTrigger 实例正确清理
- ✅ 不影响滚动性能（保持 60fps）

---

### 🎯 优化点 12：ChatInput 引用浮动按钮动画（增强小细节）

**优先级**：P3 — 锦上添花。

#### 现状

`web/src/components/ChatInput.vue` 第 200-212 行：

```vue
<Transition name="quote-pop">
  <button v-if="quoteCandidate" class="quote-float-btn" ...>+ 引用</button>
</Transition>
```

CSS 在 1242-1251 行：

```css
.quote-pop-enter-active {
  animation: quote-pop-in 0.15s var(--ease-out, cubic-bezier(0.16, 1, 0.3, 1));
}
.quote-pop-leave-active {
  animation: quote-pop-in 0.1s reverse;
}
@keyframes quote-pop-in {
  from { opacity: 0; transform: scale(0.8); }
  to { opacity: 1; transform: scale(1); }
}
```

#### 方案（可选增强）

如果想做得更精致：

```vue
<script setup lang="ts">
import { gsap, useGsap, easeMap } from '@/composables/useGsap'

const quoteFloatRef = ref<HTMLElement | null>(null)

useGsap(() => {
  watch(() => quoteCandidate.value, (val) => {
    if (val && quoteFloatRef.value) {
      gsap.fromTo(quoteFloatRef.value,
        { opacity: 0, scale: 0.6, y: 8 },
        { opacity: 1, scale: 1, y: 0, duration: 0.2, ease: easeMap.spring }
      )
    }
  }, { flush: 'post' })
}, { scope: () => quoteFloatRef.value })
</script>
```

**与旧版对比**：spring 缓动比 scale 更有"弹性"，与引用按钮的语义（弹出邀请）匹配。

**决策**：此优化点可选。如果觉得改造成本高，可保留原 CSS 方案。

---

## 四、实施路线图

按依赖关系排序，**前一步不完成，后一步不要开**：

| 阶段 | 内容 | 估时 | 风险 |
|---|---|---|---|
| **W1** | 优化点 1（基础设施） | 1 天 | 低 |
| **W1** | 优化点 2（ToolCallCard / BubbleChrome） | 1 天 | 中（影响所有工具气泡） |
| **W2** | 优化点 3（MessageBubble stagger） | 0.5 天 | 中（影响聊天主流程） |
| **W2** | 优化点 4（ChatInput 引用标签） | 0.5 天 | 低 |
| **W2** | 优化点 5（WelcomeScreen） | 0.5 天 | 低 |
| **W3** | 优化点 7（CapabilitiesView 数字 reveal） | 0.5 天 | 低 |
| **W3** | 优化点 8（MemoryView fade-up） | 0.5 天 | 低 |
| **W3** | 优化点 9（路由级过渡） | 0.5 天 | 中（影响所有路由） |
| **W4** | 优化点 10（SessionDrawer 增强） | 0.3 天 | 低 |
| **W4** | 优化点 6（SplitText 字符级） | 1.5 天 | 高（与 markdown-it 集成需测试） |
| **W5** | 优化点 11（ScrollTrigger） | 1 天 | 中（虚拟列表兼容） |
| **W5** | 优化点 12（ChatInput 浮动按钮） | 0.2 天 | 低 |

总计：约 **8 个工作日**。

---

## 五、风险评估与回滚策略

### 5.1 性能风险

| 风险 | 触发场景 | 缓解 |
|---|---|---|
| GSAP tween 过多导致 jank | 长列表 / 多组件同时动画 | 使用 `gsap.quickTo` 替代 tween；用 `overwrite: 'auto'` 防止堆积 |
| WebView2 GPU 加速异常 | transform 仍触发重绘 | 强制 `will-change: auto` 在动画结束后；或使用 `gsap.set(el, { clearProps: 'all' })` |
| SplitText 拆词增加 DOM 节点 | 流式长回复 | 仅在 `streaming === true` 时启用；结束后 `revert()` |

### 5.2 兼容性风险

| 风险 | 触发场景 | 缓解 |
|---|---|---|
| WebView2 版本旧 | 行为与 Chrome 不同 | GSAP 不依赖现代 CSS，原生 transform / opacity 即可；测试时用 Edge WebView2 runtime |
| `@starting-style` 缺失（Chrome < 117） | MessageBubble 入场 | 改用 GSAP 后不再依赖（见优化点 3） |
| `prefers-reduced-motion` 在桌面端 | 用户系统设置 | `useGsap` 已通过 `matchMedia` 全局处理；Tauri 端可暴露 toggle 强制开启 |

### 5.3 回滚策略

- 每个优化点独立分支，按需合并
- 关键 commit 标记：`<scope>(gsap): <description>` — 例如 `chat(gsap): replace ToolCallCard maxHeight with gsap.fromTo`
- 如果某优化点出现严重问题，可单点 revert 到优化前

---

## 六、验收清单（Definition of Done）

整体实施完成后，逐项确认：

### 6.1 基础验收

- [ ] `web/package.json` 增加 `gsap` 和 `@gsap/business` 依赖，`npm install` 无错
- [ ] `web/src/composables/useGsap.ts` 文件存在，导出 gsap / ScrollTrigger / SplitText / Observer / easeMap / durationMap
- [ ] 所有新代码使用 `useGsap(() => {...}, { scope: () => ref.value })` 模式
- [ ] TypeScript 编译通过：`npm run type-check`（如有该脚本）
- [ ] ESLint 通过：`cd web && npx eslint src --ext .ts,.vue`

### 6.2 功能验收

- [ ] 工具气泡展开/收起无 reflow、无闪烁
- [ ] 消息入场 stagger 节奏与现状一致（40ms / 项，最多 10 项）
- [ ] 欢迎屏 7 个区块依次入场
- [ ] 能力仪表盘数字从 0 滚动到目标
- [ ] 记忆列表项错落 fade-up
- [ ] 路由切换有 fade 过渡
- [ ] AI 回复字符级打字效果（如果启用优化点 6）

### 6.3 性能验收

- [ ] 聊天页面滚动保持 60fps（DevTools Performance 录制）
- [ ] 工具气泡展开/收起无 "Forced reflow" 警告
- [ ] GSAP 全局 timeline 在组件卸载后停止（Memory 面板无持续占用）

### 6.4 无障碍验收

- [ ] 启用系统 `prefers-reduced-motion: reduce` 后，所有 GSAP 动画 0.01s 完成
- [ ] 键盘 Tab 焦点顺序未改变
- [ ] 屏幕阅读器朗读顺序未改变
- [ ] ARIA 属性保留（aria-live、aria-modal 等）

### 6.5 跨浏览器 / 跨平台验收

- [ ] Windows Tauri 桌面端（WebView2）
- [ ] macOS / Linux Tauri 桌面端（如有支持计划）
- [ ] 开发模式浏览器调试（Chrome / Edge）
- [ ] 6 个主题切换（素影、超线、夜晚、金缮、草绿、子夜）下动画表现一致

---

## 七、不做的事（明确边界）

以下场景**不引入 GSAP**，避免过度工程化：

1. **CSS keyframes 动画**（spin / pulse / fade-in / slide） — 保留 `animations.css`
2. **Vue `<Transition>` / `<TransitionGroup>` 简单场景** — router-view 包裹的 fade 过渡仍用 CSS
3. **hover / focus / active 状态反馈** — 保留 CSS transition
4. **主题切换颜色过渡** — 已用 `html { transition: background-color 0.3s ease }` 在 App.vue
5. **keep-alive 缓存的 ChatView** — 不在路由级 transition 包裹范围内（避免状态重置）
6. **sub-agent read-only bar** — 单个 div，无需动画

---

## 八、参考实现链接

实施时如遇具体问题，查阅：

- **GSAP 官方文档**：`https://gsap.com/docs/v3/`
- **GSAP with Frameworks**：`D:\Maxma\.agents\skills\gsap-frameworks\SKILL.md`（Vue 3 模式详解）
- **GSAP Timeline**：`D:\Maxma\.agents\skills\gsap-timeline\SKILL.md`
- **GSAP ScrollTrigger**：`D:\Maxma\.agents\skills\gsap-scrolltrigger\SKILL.md`
- **GSAP Utils**：`D:\Maxma\.agents\skills\gsap-utils\SKILL.md`
- **GSAP Performance**：`D:\Maxma\.agents\skills\gsap-performance\SKILL.md`
- **项目现状索引**：`D:\Maxma\MaxmaHere\PROJECT_INDEX.md`
- **设计令牌**：`D:\Maxma\MaxmaHere\web\src\assets\styles\tokens.css`
- **现有动画定义**：`D:\Maxma\MaxmaHere\web\src\assets\styles\animations.css`

---

## 九、变更记录

| 日期 | 作者 | 变更 |
|---|---|---|
| 2026-07-31 | Claude (本会话) | 初稿：12 个优化点 + 实施路线图 + 验收清单 |

---

**下一步建议**：在动手前，先把优化点 1（基础设施）作为独立 PR 完成并验证，再批量做 2-5。这是项目的"地基"，地基不稳后续都是空中楼阁。