# Python
__pycache__/
*.py[cod]
.coverage
.pytest_cache/
*.egg-info/
/dist/
build/
*.egg

# All dist directories (build output for various packages)
dist/
dist-electron/

# Virtual environment
.venv/
venv/
env/

# IDE
.idea/
.vscode/

# CI / Tools
.github/
.trae/
*.swp
*.swo

# Environment
.env
.env.local

# Frontend
web/node_modules/
web/dist/

# OS
Thumbs.db
.DS_Store

# Claude Code
.claude/

# 用户固定会话（Const Session）持久化数据
api/data/const-sessions/

# Project specific
botpy.log
*.dll
extracted/
config/personas/USER.md
config/personas/SOUL.md
config/personas/MEMORY.md
config/personas/memory_operations.yaml
skills/bilibili/cookie.txt
tools/bilibili/cookie.txt
output/bilibili/
config/personas/memory.yaml
*.lock
!requirements.lock
*.clean

# Provider credentials (明文 API key)
providers.yaml
providers.yaml.legacy-*.bak
api/data/providers.yaml

# 路径白名单（自动生成，每用户本地配置）
api/data/path_whitelist.yaml

# MaxmaBlocker 拒止锚数据（自动生成，每用户本地配置）
api/data/maxma_blocker.yaml
api/data/MaxmaBlocker

# 认证 Token（自动生成，每用户本地配置）
api/data/auth_token.yaml

# MCP 服务器配置（每用户本地配置，可能含 API Key 等敏感信息）
config/mcp_servers.yaml
api/data/mcp_servers.yaml

# 事件钩子配置（每用户本地配置）
api/data/event_hooks.yaml

# Sticker 本地运行数据（自动生成）
api/data/sticker_favorites.yaml
api/data/sticker_preferences.yaml
api/data/sticker_recent.yaml

# Tauri 本地构建产物
desktop/src-tauri/target/
desktop/src-tauri/binaries/*.exe

# Anthropic Skills（忽略 skill 子目录，保留目录本身用于 .gitkeep）
anthropic_skills/*/

# Macros（同上）
macros/*/

# 用户上传文件和浏览器截图（运行时生成）
uploads/

# 日志文件（运行时生成）
logs/

# 安全威胁模型（本地文档，不提交）
dev_docs/security/
