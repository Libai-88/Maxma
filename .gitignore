# Python
__pycache__/
*.py[cod]
.coverage
.pytest_cache/
*.egg-info/
/dist/
/build/maxma-server/
!/build/
!/build/*.bat
!/build/*.ps1
!/build/*.spec
*.egg

# All dist directories (build output for various packages)
dist/
dist-electron/

# Virtual environment
.venv/
venv/
env/

# IDE
.idea/
.vscode/

# CI / Tools
.github/*
!.github/workflows/
!.github/workflows/*.yml
.trae/
*.swp
*.swo

# Environment
.env
.env.local

# Frontend
web/node_modules/
web/dist/

# Bun sidecar dependencies
bun-sidecar/node_modules/

# OS
Thumbs.db
.DS_Store

# Claude Code
.claude/

# 用户固定会话（Const Session）持久化数据
api/data/const-sessions/

# Project specific
botpy.log
*.dll
extracted/
config/personas/USER.md
config/personas/SOUL.md
config/personas/MEMORY.md
config/personas/memory_operations.yaml
skills/bilibili/cookie.txt
tools/bilibili/cookie.txt
output/bilibili/
config/personas/memory.yaml
*.lock
!desktop/src-tauri/Cargo.lock
*.clean

# Provider credentials (明文 API key)
providers.yaml
providers.yaml.legacy-*.bak
api/data/providers.yaml

# 凭据加密密钥（api_key 加密端点生成，绝对不能提交）
api/data/credential.key

# 路径白名单（自动生成，每用户本地配置）
api/data/path_whitelist.yaml

# MaxmaBlocker 拒止锚数据（自动生成，每用户本地配置）
api/data/maxma_blocker.yaml
api/data/MaxmaBlocker

# 认证 Token（自动生成，每用户本地配置）
api/data/auth_token.yaml

# MCP 服务器配置（每用户本地配置，可能含 API Key 等敏感信息）
config/mcp_servers.yaml
api/data/mcp_servers.yaml

# 事件钩子配置（每用户本地配置）
api/data/event_hooks.yaml

# Sticker 本地运行数据（自动生成）
api/data/sticker_favorites.yaml
api/data/sticker_preferences.yaml
api/data/sticker_recent.yaml

# Tauri 本地构建产物
desktop/src-tauri/target/
desktop/src-tauri/binaries/*.exe

# Anthropic Skills（内置 skill 子目录需要版本控制，用户自定义 skill 在打包模式写入 %APPDATA%）

# Macros（同上）
macros/*/

# 用户上传文件和浏览器截图（运行时生成）
uploads/

# 日志文件（运行时生成）
logs/

# 安全威胁模型（本地文档，不提交）
dev_docs/security/
api/data/maxma.db

# 会话状态持久化（SQLite WAL 模式，运行时生成）
checkpoints.sqlite
checkpoints.sqlite-shm
checkpoints.sqlite-wal

# 临时测试/调试输出文件
pytest_*.txt
pytest_*.xml
stderr.log
stdout.log
test_output.txt
venv_stdout.log

# 向量数据库本地缓存
vector_db/

# 生产打包：构建时下载的嵌入式运行时和资源（不进 git，但保留 .gitkeep 占位）
desktop/src-tauri/resources/runtime/*
!desktop/src-tauri/resources/runtime/.gitkeep
desktop/src-tauri/resources/assets/*
!desktop/src-tauri/resources/assets/.gitkeep

# 便携版打包产物（PyInstaller + Tauri 构建输出）
dist-portable/

# 根目录运行时资源（构建时下载的 Node/Python/uv/Playwright/ONNX 模型）
/resources/

# 编译产物（PyInstaller/Tauri 构建的 exe，不应进版本控制）
*.exe
!desktop/src-tauri/icons/*.exe

# 临时测试脚本/产物（开发期间探索性测试，非正式测试套件）
web/round*.cjs
web/tests/comprehensive_test.py
web/tests/playwright/
web/tests/screenshots/
web/test-results/

# 临时日志/覆盖率/测试输出文件
cov_*.txt
providers_test.log
test_result.xml
web/tsc-*.txt
web/vitest_*.log
web/vitest_output.txt

# Windows 误创建的 nul 文件（重定向到 NUL 的副作用）
web/nul
web/src/NUL

# LTM outbox SQLite 临时文件（WAL 模式运行时生成）
config/personas/*.sqlite3-shm
config/personas/*.sqlite3-wal
